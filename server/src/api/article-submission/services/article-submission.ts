// Simple service export - no content type needed since we're using custom controller logic
export default {
  // This service file is required by Strapi but we handle everything in the controller
  placeholder: true
};
